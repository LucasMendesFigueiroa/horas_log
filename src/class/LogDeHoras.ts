class LogDeHoras {
  titulo: string;
  nome: string;
  tempo: string;
  descricao: string;

  constructor(titulo: string, nome: string, tempo: string, descricao: string) {
    this.titulo = titulo;
    this.nome = nome;
    this.tempo = tempo;
    this.descricao = descricao;
  }
}
